# Glintgrove 멀티에이전트 개발 사이클 로그

> 초기 개발 사이클의 역사 기록입니다. 아래 레벨 수·테스트 수·기본 언어·그래픽 설명은 각 사이클 당시 상태이며, 현재 기능과 실행 방법은 [README](README.md), 제작 문서는 [문서 안내](docs/README.md)를 따릅니다.

본 프로젝트는 **분석(Analyst) → 설계(Architect) → 개발(Developer) → 리뷰(Reviewer)** 4역할 에이전트 파이프라인으로, 자동 검증 게이트(`tools/verify-all.sh`) 기준 **버그 0건**이 연속 확인될 때까지 반복 개발되었습니다.

## 파이프라인 구조

```
┌─① 분석─┐   ┌─② 설계─┐   ┌─③ 개발─┐   ┌─④ 리뷰─┐
│ 코드 전수  │ → │ 수정 설계 │ → │ 구현    │ → │ 검수    │──┐
│ 버그 탐색  │   │ 의사결정  │   │        │   │ 회귀 점검│  │
└─────────┘   └─────────┘   └─────────┘   └────────┘  │
      ▲                                               │ 버그>0
      └───────────────────────────────────────────────┘
                        ▼ 버그=0 (verify-all ALL GREEN)
                   git push (운영 배포)
```

## 사이클 기록

### 베이스라인 구축 (커밋 4a50735)
- 빔 엔진/솔버/26레벨/렌더러/오디오/UI 구현
- 초기 반복에서 엔진·레벨 데이터 결함 다수 발견·수정:
  - visited 세트 이중 등록으로 모든 레벨 UNSOLVABLE 판정 (엔진 시드 버그)
  - `meta.splitOrient` 미적용 (리라이트 누락)
  - 레벨 문자열 `\/` 오타로 슬래시 미러 오삽입, 행 길이 불일치(ragged rows) 다수
  - L4~L26 설계 결함: 분할기 출력 방향 착각, 이미터 누락, 포탈 짝 누락, 초기치=최종치 사전 해결 상태 등 → 디버거 도구로 전수 재검증
- Chrome 헤드리스 E2E 도입 과정에서 실제 브라우저 버그 발견·수정:
  - ui.js showWin null 참조
  - 승리 후 reset 시 `won` 플래그 미초기화 (재도전 불가)
  - main.js에서 Game에 hooks 미주입 (onWin/onWinUi 무동작)
  - RAF 스로틀 환경 대응: 승리 UI 트리거를 프레임 카운트 기반으로 견고화

### 사이클 1 — 실무 품질 분석 → 보완 설계 → 구현 → 검수
- **① 분석**: 12건 발견 (major 5 / minor 5 / polish 2)
  - 힌트 사용 시 보드 상태 강제 초기화 / HUD 이동수 동결 /
    단조 satisfied 세트로 어두운 보드에서 승리 가능 / 모션 토글 무효 /
    모달 열림 중 키 입력 누수 / 소형 화면 클리핑 / 세이브 타입 미검증 /
    이중 트레이서 드리프트 / portalJump undefined 키 / 사운드·포자 배선 누락 등
- **② 설계**: 승리 판정 의미론 결정(하이브리드 — 깨어남은 지속, 승리는 순간 동시 점등),
  트레이서 통합(engine.traceCore 단일 경로), 11단계 구현 계산서 + 신규 테스트 4종 명세
- **③ 개발**: 11단계 전부 구현, 테스트 37종으로 확장
- **④ 리뷰어**: 변경 10항목 전부 OK 판정, 회귀 0건,
  배포 차단 1건(.DS_Store 추적) 및 개선 7건 지적 → 전부 반영

### 최종 게이트
| 검증 | 결과 |
|---|---|
| 문법 (`node --check` × 10파일) | PASS |
| 레벨 린터+솔버 (26레벨, par≥optimal) | PASS |
| 유닛/스모크/시맨틱 (`node --test`) | 37/37 PASS |
| 브라우저 E2E (헤드리스 Chrome) | 16/16 PASS |
| 종합 (`tools/verify-all.sh`) | **ALL GREEN - 0 BUGS** |

반복 집행: verify-all 게이트 및 개별 스위트가 개발 전반에 걸쳐 수십 회 재실행되었으며(레벨 좌표 수정만 30회 이상의 red→green 루프),
모든 에이전트 사이클 종료 시점 기준 **버그 0건**을 확인 후 운영 배포(git push)를 진행했습니다.

## 배포 (GitHub Pages)

정적 사이트이므로 GitHub Pages로 즉시 운영 가능합니다.

1. 저장소 Settings → Pages
2. Source: `main` branch / `(root)`
3. Save → `https://kyungseok-lee.github.io/glintgrove/` 활성화 (수 분 소요)

업데이트 배포는 `main` push 시 자동 반영됩니다.

### 사이클 2 — 대규모 아키텍처 리팩토링 (이중 에이전트 평가 기반)

독립 평가 에이전트 2종이 병렬 감사를 수행했습니다.

- **FE 시니어 에이전트**: 13건 발견 (치명 3 / 높음 5 / 중간 5 / 낮음 3)
  - 치명: package.json 부재, eval 기반 테스트 적재 6곳, CI-로컬 게이트 중복
  - 높음: Game 신클래스 + Renderer 686줄 모놀리스, 매직 타일 문자 산탄, 무음 catch+에러 핸들러 부재, 논리/애니 상태 미분리, CSP 부재
- **게임 PO 에이전트**: 종합 판정 "조건부 — 완성된 패키지지 서비스가 아님"
  - **실제 결함 발견**: undo 시 이동수 환불 → ★3 무한 치팅 가능
  - 백로그 TOP8: 계측 → 경제 정합성 → 딥링크 공유 → 영문화 → 일일 챌린지 → 업적

#### 리팩토링 실행 내역

| 영역 | 이전 (v1.0) | 이후 (v1.1) |
|---|---|---|
| 모듈 | IIFE + `window.GG` 전역 + script 태그 10개 순서 로딩 | 네이티브 ES Modules (`type="module"` 단일 엔트리) |
| 구조 | js/ 플랫 10파일 | `src/` 도메인 분리 (core/sim/data/state/fx/render/game/ui/services/infra) |
| 테스트 적재 | `(0,eval)(readFileSync)` × 6곳 + DOM 스텁 2벌 중복 | ESM 직접 import + `tests/helpers/domStub.mjs` 공유 |
| 타일 어휘 | 문자 리터럴('T','f','s'...) 파일별 산탄 | `core/tiles.js` 단일 진실 |
| Renderer | 686줄 단일 클래스 | layout/background/beams/entities 분리, 배경+암석 베이크 캐싱 |
| 상태 | spin 애니가 엔티티 오염 | FX 맵 분리, Emitter 이벤트 버스 도입 |
| 세이브 | v1 | v2 (일일/업적/팁 필드) + **레거시 키 폴백 마이그레이션** |
| 보안/CSP | 없음 | CSP 메타, Referrer-Policy, 인라인 스타일 제거 |
| 관측성 | 없음 | 글로벌 에러 핸들러 + analytics 링버퍼(로컬) |
| 서비스 기능 | 없음 | **딥링크(?level=N)·공유 / 일일 챌린지(솔버 검증 절차생성) / 업적 9종 / ko-en i18n / PWA(sw+manifest)** |
| 치팅 | undo 시 moves 차감 → ★3 무한 획득 | undo는 보드만 복원, 이동수 불환불 |

#### 리뷰어 최종 검수에서 잡힌 회귀 (게이트 외부)

1. `winUiDone`이 startLevel/resetLevel에서 리셋되지 않아 두 번째 승리부터 오버레이 미표시 → 수정 + 재발 방지 테스트 추가
2. 세이브 v1→v2 마이그레이션이 실제로는 도달 불가(v2 키만 조회) → 레거시 키 폴백 + 즉시 v2 저장으로 수정 + 키명 검증 테스트 추가

교훈: E2E가 단일 승리 흐름만 커버하면 '반복 플레이' 회귀는 못 잡는다 — 리뷰어의 코드 대조 독립 검증이 결정적이었다.

### 사이클 3 — PO 백로그 잔여분 전부 소화 (v1.2)

| 백로그 | 구현 |
|---|---|
| 콘텐츠 확장 | **챕터 5 「다시 찾은 새벽」 L27~L30 추가** (총 30레벨). 그리드를 좌표 맵→문자열 생성기로 만들어 수작업 오타 원천 차단, 솔버 게이트 전수 통과 |
| 리텐션 | 일일 챌린지(날짜 시드 + 스크램블 + 솔버 실시간 optimal 계산, 스트릭 저장), 업적 9종 패널+토스트 |
| 바이럴 | ?level=N 딥링크 + Web Share/클립보드 공유 버튼 |
| 글로벌 | ko/en 사전 레이어(50키×2), auto 감지, 설정 전환 즉시 반영 |
| LiveOps | config.json 원격 플래그(parOverrides/힌트 패널티/일일 난이도 범위), 로컬 analytics 링버퍼 + JSON 내보내기 버튼 |
| FTUE | L1 포인터 가이드, L6/L17/L23 메커니즘 카드, 25초 무조작 힌트 제안 |
| 코스메틱 | 빛 스킨 4종(별 개수 해금), 색상 팔레트 전역 적용 |

E2E 확장: 일일 챌린지 시작/검증, 튜토리얼 카드 표시/해제, 스킨 셀렉트 렌더 (+6 체크)
최종 게이트: 유닛 48/48 · E2E 17/17 · 레벨 30 솔버 검증 · ALL GREEN

### 사이클 4 — 성능·접근성·운영 안정화 (CI 실패 대응 포함)

- **CI 실패 근본 수정**: 워크플로 문법 검사가 리팩토링 전 `js/*` 경로를 가리켜 실패 → `find src`로 수정, E2E 스텝 크롬 탐지 폴백/서버 정리 정비. push 후 GitHub Actions **success** 확인
- **렌더 성능**: 
  - 빔 코어의 매 프레임 shadowBlur 제거 → 색상별 프리렌더 글로우 스프라이트(`beams.js`)로 교체, 세그먼트당 3회 stroke + shadow → 2회 stroke + drawImage 1회
  - 엔티티 헤일로(발산원/나무/꽃/올빼미/승리 플래시)의 매 프레임 createRadialGradient 5곳 → `(반경|색)` 키 GradientCache로 캐싱
  - 스킨 변경 시 스프라이트·그라디언트 캐시 무효화(`game.applySkin`)
- **배터리/사운드 예절**: visibilitychange로 탭 비활성 시 앰비언트 정지, 복귀 시 재개 (+세션 이벤트 계측)
- **접근성**: 모달 열림 시 기본 버튼 자동 포커스, 닫힘 시 이전 포커스 복원
- **테스트**: 업적 서비스 5종 추가 (유닛 53종)

### 사이클 5 — v2.0: 300 라운드 & Tetris Effect 톤 그래픽 (사용자 요구)

- **300 레벨**: 템플릿 아키타입 8종(지그재그/트윈스/캐스케이드/포탈박스/게이트런/나선/허브/듀얼) + **ForcedHub**(강제 경로 트리 — optimal = 장치 수 보장) + **핸드메이드 변환**(대칭/전치 — solvability 보존 검증) + 검증된 레벨 재활용 폴백(재검증). 생성 8.8초, 전부 솔버 검증
- 생성기 버그 사냥: 대각선 웨이포인트(빛 도달 불가 구조), solved-상태 기준 후보(opt 0), splitOrient 미반전, 지터에 의한 장치 유실, 이전 세대 손상 레벨 재복사 연쇄 — 모두 해결
- **그래픽 v2**: Bloom 패스(쿼터해상도), 네뷸라+십자글린트 별+오로라 리본 배경, 빔 reveal/width wobble, 각성 시 광선+모트+블룸 펄스, 클리어 시 슬로모션+캐스케이드
- **수익화 제거**: 스킨 전면 무료
- 난이도 4단계 표기(쉬움/보통/어려움/매우 어려움) 레벨 선택 UI

### 사이클 6 — 리뷰어-개발자 협업 버그 제거 + 영문 기본화 (v2.1)

리뷰어 에이전트 전수 조사 → **12건 확정 (CRITICAL 2 / MAJOR 2 / MINOR 8)** → 개발자 전항목 수정 → 리뷰어 재검증 **12/12 PASS**.

- CRITICAL: 힌트 텍스트가 meta 경로 미스로 절대 표시 안 되던 버그 / 튜토리얼 카드가 pointer-events 상속으로 닫기 불가(매 진입 재표시)
- MAJOR: 일일 챌린지 HUD 라벨 첫 클릭 후 기본명 복귀 / 영어 UI에서 챕터명·난이도 태그 한국어 노출
- MINOR: 승리 폭죽 좌표 스케줄 확정, parOverrides 실제 적용, 일일 재도전 config 연동, daily 폴백 orients, meta 좌표 드리프트 51건(린트 추가+데이터 수정), og/초기 표기 갱신, resetLevel 음높이 초기화, SW navigate network-first
- **영문 기본화**: 기본 언어 en (언어 설정에서 한국어 유지 가능), 챕터 15개 nameEn/descEn 완비
- 재발 방지: check-levels에 meta 좌표 린트 추가, 세이브 테스트 EN 기대값 갱신
